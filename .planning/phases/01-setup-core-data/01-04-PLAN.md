---
phase: 01-setup-core-data
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - Services/BazaarFetcherService.cs
  - appsettings.json
autonomous: true
requirements: []

must_haves:
  truths:
    - EF uses navigation property for FK (not explicit Id) when creating new BazaarItems
    - Dead _fetchIntervalSeconds / FetchIntervalSeconds config removed
    - MinDelayAfterSnapshotSeconds read as double (JSON 9.5) and applied as TimeSpan for delay and timer
  artifacts:
    - path: Services/BazaarFetcherService.cs
      provides: PriceSnapshot.BazaarItem navigation, TimeSpan delay, no dead interval field
    - path: appsettings.json
      provides: MinDelayAfterSnapshotSeconds fractional seconds; no FetchIntervalSeconds

---

<objective>
Apply follow-up fixes:
1. EF relationship — navigation property instead of explicit FK before SaveChanges
2. Remove dead FetchIntervalSeconds config
3. Fractional delay — GetValue double for MinDelayAfterSnapshotSeconds, TimeSpan for Task.Delay and Timer
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Fix EF relationship and config</name>
  <files>
    - Services/BazaarFetcherService.cs
    - appsettings.json
  </files>
  <action>
    1. BazaarFetcherService.cs:
       - Remove _fetchIntervalSeconds field (line 25)
       - Remove FetchIntervalSeconds from ctor (line 43)
       - Change line ~167: `BazaarItemId = bazaarItem.Id` → `BazaarItem = bazaarItem`
       
    2. appsettings.json:
       - Remove "FetchIntervalSeconds": 300
       - Update MinDelayAfterSnapshotSeconds comment to note 9.5 approximation
  </action>
  <verify>
    <automated>dotnet build SkyBazaar.csproj</automated>
  </verify>
  <done>
    EF handles FK ordering correctly. Dead config removed.
  </done>
</task>

</tasks>

<verification>
- [ ] dotnet build succeeds
- [ ] EF uses navigation property
- [ ] Dead config removed
</verification>

<success_criteria>
1. EF uses navigation property for FK when creating new BazaarItems
2. Dead _fetchIntervalSeconds config removed
3. MinDelay default changed to 9.5
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-core-data/01-04-SUMMARY.md`
