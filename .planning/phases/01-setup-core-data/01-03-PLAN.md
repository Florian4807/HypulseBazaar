---
phase: 01-setup-core-data
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - Program.cs
  - Services/BazaarFetcherService.cs
  - Services/HypixelApiService.cs
  - Models/PriceSnapshot.cs
  - appsettings.json
  - SkyBazaar.csproj
autonomous: true
requirements: []

must_haves:
  truths:
    - Polling uses ~9.5s delay after successful store (configurable), not 300s Timer as main gate
    - HypixelApiService registered with AddHttpClient, not AddSingleton
    - BuyMovingWeek/SellMovingWeek XML comments correctly describe volume stats
    - HypixelApiService uses top-of-book (summary first entry) when it differs from quick_status
    - DB writes batch SaveChanges at end, not per-item in loop
    - BazaarItem.UpdatedAt set to snapshot timestamp for existing items
    - MessagePack updated to version addressing GHSA-4qm4-8hg2-g2xm
  artifacts:
    - path: Program.cs
      provides: AddHttpClient registration for IHypixelApiService
    - path: Services/BazaarFetcherService.cs
      provides: Polling cadence fix, batch DB writes, UpdatedAt update
    - path: Services/HypixelApiService.cs
      provides: Top-of-book logic from summaries
    - path: Models/PriceSnapshot.cs
      provides: Fixed XML comments for moving week fields
    - path: appsettings.json
      provides: MinDelayAfterSnapshotSeconds config
    - path: SkyBazaar.csproj
      provides: Updated MessagePack version
  key_links:
    - from: Program.cs
      to: Services/HypixelApiService.cs
      via: AddHttpClient DI
      pattern: "IHypixelApiService"
---

<objective>
Apply 7 specific hardening fixes to SkyBazaar backend:
1. Polling cadence - use ~9.5s delay after success, not 300s Timer
2. HttpClient DI - AddHttpClient instead of AddSingleton
3. Fix PriceSnapshot XML comments for BuyMovingWeek/SellMovingWeek
4. Top-of-book parity - use summary first entry when different from quick_status
5. Batch DB writes - single SaveChanges at end of loop
6. Set BazaarItem.UpdatedAt on existing items
7. Update MessagePack to patched version
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
Reference: temp-skyupdater/BazaarUpdater.cs - Coflnet polling pattern
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix HttpClient DI and polling cadence</name>
  <files>
    - Program.cs
    - Services/BazaarFetcherService.cs
    - appsettings.json
  </files>
  <action>
    1. Program.cs: Change AddSingleton to AddHttpClient:
       ```csharp
       builder.Services.AddHttpClient<IHypixelApiService, HypixelApiService>();
       ```

    2. appsettings.json: Add MinDelayAfterSnapshotSeconds config:
       ```json
       "MinDelayAfterSnapshotSeconds": 10
       ```

    3. BazaarFetcherService.cs: Replace timer-based polling with long-lived loop:
       - Remove 300s timer interval as main throttle
       - After successful store, delay MinDelayAfterSnapshotSeconds before polling
       - Keep inner poll-until-LastUpdated loop
       - Timer becomes short wake-up (~10s) only for cancellation check
  </action>
  <verify>
    <automated>dotnet build SkyBazaar.csproj</automated>
  </verify>
  <done>
    HttpClient properly registered with AddHttpClient. Polling delays ~9.5-10s after success.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix PriceSnapshot XML comments and add top-of-book logic</name>
  <files>
    - Models/PriceSnapshot.cs
    - Services/HypixelApiService.cs
  </files>
  <action>
    1. Models/PriceSnapshot.cs: Fix comments:
       - BuyMovingWeek: "7-day rolling buy volume" (not price average)
       - SellMovingWeek: "7-day rolling sell volume" (not price average)

    2. HypixelApiService.cs: Add top-of-book logic:
       - If SellSummary has entries, use first PricePerUnit as sell reference
       - If BuySummary has entries, use first PricePerUnit as buy reference
       - Document in code comments
  </action>
  <verify>
    <automated>dotnet build SkyBazaar.csproj</automated>
  </verify>
  <done>
    Comments corrected. Top-of-book logic mirrors Coflnet behavior.
  </done>
</task>

<task type="auto">
  <name>Task 3: Batch DB writes and set UpdatedAt</name>
  <files>
    - Services/BazaarFetcherService.cs
  </files>
  <action>
    In BazaarFetcherService.ExecuteFetchAsync:

    1. Batch DB writes:
       - Collect new BazaarItems to add
       - Add all without SaveChanges in loop
       - Single SaveChanges after loop for all items

    2. Set UpdatedAt:
       - When snapshot inserted for existing BazaarItem, set UpdatedAt = timestamp
  </action>
  <verify>
    <automated>dotnet build SkyBazaar.csproj</automated>
  </verify>
  <done>
    DB writes batched. Existing items get UpdatedAt set.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update MessagePack and verify build</name>
  <files>
    - SkyBazaar.csproj
  </files>
  <action>
    1. Check current MessagePack version in SkyBazaar.csproj
    2. Update to latest patched version (2.1.x or higher for security fix GHSA-4qm4-8hg2-g2xm)
    3. Run dotnet restore and dotnet build
    4. Verify serialization still works for Order blobs
  </action>
  <verify>
    <automated>dotnet build SkyBazaar.csproj</automated>
  </verify>
  <done>
    MessagePack updated. Build succeeds.
  </done>
</task>

</tasks>

<verification>
- [ ] dotnet build succeeds
- [ ] All 7 fixes implemented
- [ ] No regressions in existing functionality
</verification>

<success_criteria>
1. Polling uses ~9.5s delay after successful store (configurable), not 300s Timer as main gate
2. HypixelApiService registered with AddHttpClient, not AddSingleton
3. BuyMovingWeek/SellMovingWeek XML comments correctly describe volume stats
4. HypixelApiService uses top-of-book (summary first entry) when it differs from quick_status
5. DB writes batch SaveChanges at end, not per-item in loop
6. BazaarItem.UpdatedAt set to snapshot timestamp for existing items
7. MessagePack updated to version addressing GHSA-4qm4-8hg2-g2xm
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-core-data/01-03-SUMMARY.md`
