---
phase: 01-setup-core-data
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - SkyBazaar.csproj
  - Program.cs
  - Startup.cs
  - Models/BazaarItem.cs
  - Data/SkyBazaarDbContext.cs
  - Services/HypixelApiService.cs
  - Services/BazaarFetcherService.cs
  - appsettings.json
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
  - CORE-05
  - STOR-01
  - STOR-02
  - STOR-03
  - STOR-04

must_haves:
  truths:
    - Service fetches bazaar data from Hypixel API successfully
    - Service parses item names, buy/sell prices, and quantities correctly
    - Service stores each bazaar snapshot in database with timestamp
    - Service runs on configurable schedule (default every 5 minutes)
    - Service handles API rate limits gracefully with retry logic
    - Historical price data is queryable from database by item name
    - Price history includes buy price, sell price, and moving average
    - Data retention is configurable (default 30 days)
  artifacts:
    - path: SkyBazaar.csproj
      provides: Project configuration with EF Core and System.Text.Json
    - path: Models/BazaarItem.cs
      provides: Data model for bazaar items with price history
    - path: Data/SkyBazaarDbContext.cs
      provides: EF Core DbContext for SQLite/PostgreSQL
    - path: Services/HypixelApiService.cs
      provides: HTTP client to fetch and parse Hypixel bazaar API
    - path: Services/BazaarFetcherService.cs
      provides: Background service for scheduled fetching with rate limiting
    - path: appsettings.json
      provides: Configuration for fetch interval and retention days
  key_links:
    - from: Services/BazaarFetcherService.cs
      to: Services/HypixelApiService.cs
      via: Dependency injection
      pattern: "IBazaarApiService"
    - from: Services/BazaarFetcherService.cs
      to: Data/SkyBazaarDbContext.cs
      via: DbContext injection
      pattern: "SkyBazaarDbContext"
    - from: Services/HypixelApiService.cs
      to: Models/BazaarItem.cs
      via: JSON deserialization
      pattern: "JsonSerializer.Deserialize"
---

<objective>
Set up the core data pipeline: fetch Hypixel bazaar data, parse it, and store in local database with scheduled polling.

Purpose: Establish the foundation for the entire application - without stored data, nothing else works.
Output: Working background service that fetches and stores bazaar data every 5 minutes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@temp-skybazaar/Models/StorageQuickStatus.cs
@temp-skybazaar/Services/BazaarService.cs
@temp-skybazaar/SkyBazaar.csproj

# Hypixel API: GET https://api.hypixel.net/v2/skyblock/bazaar
# No authentication required for public endpoint
# Rate limit: ~60 requests/minute recommended
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create new .NET project structure with EF Core</name>
  <files>
    - SkyBazaar.csproj
    - Program.cs
    - Startup.cs
    - appsettings.json
  </files>
  <action>
    Create a clean .NET project at the root level:
    
    1. Create SkyBazaar.csproj targeting net9.0 with:
       - Microsoft.AspNetCore.App framework
       - Microsoft.EntityFrameworkCore.Sqlite (for local dev)
       - Microsoft.EntityFrameworkCore.Design (for migrations)
       - NodaTime (for reliable timestamps)
       - Polly (for retry/rate-limit handling)
    
    2. Create Program.cs with:
       - Host.CreateDefaultBuilder(args)
       - DbContext registration
       - Background service registration
       
    3. Create Startup.cs (or configure in Program.cs):
       - Add DbContext to DI
       - Add HypixelApiService to DI
       - Add BazaarFetcherService as hosted service
       
    4. Create appsettings.json with:
       - ConnectionStrings:DefaultConnection (SQLite file path)
       - Bazaar:FetchIntervalSeconds: 300 (5 minutes)
       - Bazaar:RetentionDays: 30
       - Bazaar:MaxRetries: 3
       - Bazaar:RetryDelaySeconds: 10

    Remove all Coflnet, Cassandra, Kafka, Redis dependencies from original project.
  </files>
  <verify>
    <automated>dotnet build SkyBazaar.csproj</automated>
  </verify>
  <done>
    Project builds successfully with EF Core and clean dependencies.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create database models and DbContext</name>
  <files>
    - Models/BazaarItem.cs
    - Models/PriceSnapshot.cs
    - Data/SkyBazaarDbContext.cs
  </files>
  <action>
    Create data models and EF Core context:
    
    1. Models/BazaarItem.cs:
       - Id (int, PK, auto-increment)
       - ProductId (string, unique) - e.g., "ENCHANTED_COBBLESTONE"
       - Name (string) - display name
       - CreatedAt (DateTime)
       - UpdatedAt (DateTime)
       
    2. Models/PriceSnapshot.cs:
       - Id (int, PK, auto-increment)
       - BazaarItemId (int, FK to BazaarItem)
       - Timestamp (DateTime with index)
       - BuyPrice (decimal)
       - SellPrice (decimal)
       - BuyVolume (long)
       - SellVolume (long)
       - BuyMovingWeek (long) - 7-day moving average
       - SellMovingWeek (long)
       - BuyOrdersCount (int)
       - SellOrdersCount (int)
       
    3. Data/SkyBazaarDbContext.cs:
       - DbSet&lt;BazaarItem&gt; Items
       - DbSet&lt;PriceSnapshot&gt; Snapshots
       - OnModelCreating: Configure composite index on (BazaarItemId, Timestamp)
       - OnModelCreating: Configure cascade delete for snapshots when item deleted
       
    Reference patterns from temp-skybazaar/Models/StorageQuickStatus.cs for field mapping.
  </files>
  <verify>
    <automated>dotnet ef migrations add InitialCreate --project . && dotnet ef database update --project .</automated>
  </verify>
  <done>
    SQLite database created with Items and Snapshots tables. Migrations apply successfully.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Hypixel API service</name>
  <files>
    - Services/HypixelApiService.cs
    - Models/HypixelBazaarResponse.cs
  </files>
  <action>
    Create service to fetch and parse Hypixel bazaar API:
    
    1. Models/HypixelBazaarResponse.cs:
       - Root response with Products dictionary
       - ProductInfo class matching Hypixel API structure:
         - productId
         - buySummary (List of OrderSummary)
         - sellSummary (List of OrderSummary)
         - quickStatus: { productId, sellPrice, buyPrice, sellVolume, buyVolume, sellMovingWeek, buyMovingWeek, sellOrders, buyOrders }
         
    2. Services/HypixelApiService.cs (interface IBazaarApiService):
       - HttpClient with base URL https://api.hypixel.net/v2/skyblock/bazaar
       - GetBazaarAsync() returning List&lt;BazaarItemSnapshot&gt;
       - Handle HttpRequestException with retry logic
       - Parse buy/sell prices from quickStatus (instant orders)
       - Parse order summaries (instant buy/sell offers)
       
    Use System.Text.Json for parsing (not Newtonsoft).
    Implement retry with Polly for transient failures.
    Log all API calls and errors.
  </files>
  <verify>
    <automated>dotnet test --filter "Category=Unit" 2>/dev/null || echo "No tests yet - verify manually: dotnet run and check logs"</automated>
  </verify>
  <done>
    Service can fetch and parse Hypixel bazaar data. API response properly deserialized.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create background fetcher service with scheduling</name>
  <files>
    - Services/BazaarFetcherService.cs
  </files>
  <action>
    Create IHostedService for periodic bazaar fetching:
    
    1. Services/BazaarFetcherService.cs:
       - Implement IHostedService, IAsyncDisposable
       - Inject IBazaarApiService and SkyBazaarDbContext
       - Inject IConfiguration for interval and retention settings
       
    2. StartAsync:
       - Load configuration (FetchIntervalSeconds, RetentionDays)
       - Start timer with configurable interval (default 5 min)
       - Run initial fetch on startup
       
    3. ExecuteFetchAsync:
       - Call IBazaarApiService.GetBazaarAsync()
       - For each product in response:
         - Get or create BazaarItem by ProductId
         - Create PriceSnapshot with current prices
         - Save to database
       - Commit transaction after all items
       - Log fetch success with item count
       
    4. HandleRateLimits:
       - If 429 response, wait RetryDelaySeconds and retry
       - Max retries from config (default 3)
       
    5. DataRetentionCleanup:
       - On each fetch, delete snapshots older than RetentionDays
       - Use bulk delete for efficiency
       
    Use ILogger for structured logging.
    Make fetch interval configurable via appsettings.json.
  </files>
  <verify>
    <automated>dotnet run --project . --urls "http://localhost:5000" &amp; sleep 10 &amp; curl -s http://localhost:5000/api/bazaar 2>/dev/null | head -c 200; kill %1 2>/dev/null</automated>
  </verify>
  <done>
    Background service runs, fetches data every 5 minutes, stores in database.
  </done>
</task>

</tasks>

<verification>
- [ ] dotnet build succeeds
- [ ] Database migrations apply cleanly
- [ ] Service starts without errors
- [ ] Initial fetch completes and data appears in database
- [ ] Scheduled fetches run at configured interval
- [ ] Old data is cleaned up based on retention setting
- [ ] API endpoint returns stored data (Phase 2 will add this)
</verification>

<success_criteria>
Service runs as a standalone .NET application that:
1. Fetches bazaar data from Hypixel API (CORE-01, CORE-02)
2. Stores snapshots in database with timestamp (CORE-03)
3. Runs on configurable schedule (CORE-04)
4. Handles rate limits gracefully (CORE-05)
5. Historical data is queryable (STOR-02)
6. Data includes buy/sell prices and moving averages (STOR-03)
7. Data retention is configurable (STOR-04)
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-core-data/01-01-SUMMARY.md`
</output>
