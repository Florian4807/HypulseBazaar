---
phase: 02-api-flip-analysis
plan: "02"
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - Services/FlipDetectionService.cs
  - Models/FlipRecommendationDto.cs
  - Controllers/FlipsController.cs
autonomous: true
requirements: [FLIP-01, FLIP-02, FLIP-03]
must_haves:
  truths:
    - "Profit margin calculated correctly (sell price - buy price)"
    - "Items sorted by best profit margins"
    - "Top flips endpoint returns recommended items sorted by profit potential"
  artifacts:
    - path: "Services/FlipDetectionService.cs"
      provides: "Flip calculation logic"
      methods: ["CalculateMargin", "GetTopFlips", "GetFlipForItem"]
    - path: "Models/FlipRecommendationDto.cs"
      provides: "Flip response DTOs"
      properties: ["ProfitMargin", "ProfitPercentage", "RecommendationScore"]
    - path: "Controllers/FlipsController.cs"
      provides: "Flip API endpoints"
      exports: ["GET /api/flips", "GET /api/flips/{productId}"]
  key_links:
    - from: "Services/FlipDetectionService.cs"
      to: "SkyBazaarDbContext"
      via: "constructor injection"
      pattern: "SkyBazaarDbContext.*DbContext"
    - from: "Controllers/FlipsController.cs"
      to: "FlipDetectionService"
      via: "constructor injection"
      pattern: "IFlipDetectionService"
---

<objective>
Calculate profitable flip opportunities and expose via API endpoints.

Purpose: Identify items where sell price significantly exceeds buy price (potential profit).

Output: Flip detection service + GET /api/flips endpoint
</objective>

<context>
@.planning/phases/02-api-flip-analysis/02-01-PLAN.md
@Data/SkyBazaarDbContext.cs
@Models/PriceSnapshot.cs

<!-- Dependencies -->
- Phase 2 Plan 1 creates the API structure this plan extends
- PriceSnapshot model has: BuyPrice, SellPrice, BuyVolume, SellVolume
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FlipCalculation DTO</name>
  <files>Models/FlipRecommendationDto.cs</files>
  <action>
Create `Models/FlipRecommendationDto.cs`:

- `FlipRecommendationDto`: ProductId, ProductName, BuyPrice, SellPrice, ProfitMargin, ProfitPercentage, VolumeScore, RecommendationScore
  - ProfitMargin = SellPrice - BuyPrice
  - ProfitPercentage = ((SellPrice - BuyPrice) / BuyPrice) * 100
  - VolumeScore = combined buy+sell volume (for filtering low-liquidity items)
  - RecommendationScore = weighted combination of profit and volume

- `FlipsResponseDto`: List<FlipRecommendationDto>, GeneratedAt, TotalItemsAnalyzed
</action>
  <verify>
File exists with proper class definitions
  </verify>
  <done>DTO ready for flip service</done>
</task>

<task type="auto">
  <name>Task 2: Create FlipDetectionService</name>
  <files>Services/FlipDetectionService.cs</files>
  <action>
Create `Services/FlipDetectionService.cs`:

1. **CalculateProfitMargin** (FLIP-01)
   - Method: `decimal CalculateMargin(decimal buyPrice, decimal sellPrice)`
   - Returns: SellPrice - BuyPrice

2. **GetTopFlips** (FLIP-02, FLIP-03)
   - Method: `Task<List<FlipRecommendationDto>> GetTopFlipsAsync(int count = 50, decimal minProfitPercent = 1.0)`
   - Query latest snapshot for each item
   - Filter out items with profit percentage below minProfitPercent
   - Sort by RecommendationScore descending
   - Formula: `RecommendationScore = ProfitMargin * Math.Log10(BuyVolume + SellVolume + 1)`

3. **GetFlipsByItem** (helper for single item analysis)
   - Method: `Task<FlipRecommendationDto?> GetFlipForItemAsync(string productId)`

Inject `SkyBazaarDbContext` via constructor. Use EF Core to query latest snapshots efficiently.

Key filtering:
- Exclude items with 0 volume (no real trading)
- Exclude items where BuyPrice > SellPrice (no arbitrage)
- Minimum profit threshold to filter noise
  </action>
  <verify>
<automated>dotnet build 2>&1 | head -30</automated>
  </verify>
  <done>Flip detection logic implemented with profit calculations</done>
</task>

<task type="auto">
  <name>Task 3: Create FlipsController endpoint</name>
  <files>Controllers/FlipsController.cs</files>
  <action>
Create `Controllers/FlipsController.cs`:

**GET /api/flips** (FLIP-03)
- Query params: `count` (default 50), `minProfitPercent` (default 1.0)
- Returns: `FlipsResponseDto` with sorted flip recommendations

**GET /api/flips/{productId}**
- Returns: Single `FlipRecommendationDto` for that item

Inject `FlipDetectionService` into controller. Use `[FromQuery]` for optional parameters.

Endpoint returns items sorted by profit potential (best first).
  </action>
  <verify>
<automated>dotnet build succeeds</automated>
  </verify>
  <done>Flip API endpoint returns sorted profit opportunities</done>
</task>

</tasks>

<verification>
- [x] dotnet build succeeds with new service and controller
- [x] Profit margin calculated correctly (sell - buy)
- [x] Items sorted by best profit margins
- [x] Top flips endpoint returns recommended items
</verification>

<success_criteria>
Flip detection calculates profit correctly. All 3 requirements (FLIP-01, FLIP-02, FLIP-03) satisfied. API returns items sorted by profit potential.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-flip-analysis/02-02-SUMMARY.md`
</output>
