---
phase: 03-frontend
plan: "01"
type: execute
wave: 1
depends_on: [02-01, 02-02]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/Sidebar.tsx
  - frontend/src/components/ItemsList.tsx
  - frontend/src/components/PriceChart.tsx
  - frontend/src/components/TopFlips.tsx
  - frontend/src/types/api.ts
  - frontend/src/services/api.ts
  - frontend/index.html
  - frontend/vite.config.ts
  - frontend/package.json
autonomous: true
requirements: [FRNT-01, FRNT-02, FRNT-03, FRNT-04]
must_haves:
  truths:
    - "Frontend displays list of all bazaar items with current buy/sell prices"
    - "User can select an item and view price history chart over time"
    - "Frontend displays top flip recommendations with profit calculations"
    - "User can filter/search items by name"
  artifacts:
    - path: "frontend/index.html"
      provides: "Entry HTML with title and root div"
    - path: "frontend/src/App.tsx"
      provides: "Main app with routing and dashboard layout"
    - path: "frontend/src/components/Sidebar.tsx"
      provides: "Navigation: Items, Flips, History tabs"
    - path: "frontend/src/components/ItemsList.tsx"
      provides: "Searchable items list (FRNT-01, FRNT-04)"
    - path: "frontend/src/components/PriceChart.tsx"
      provides: "Chart.js line chart for price history (FRNT-02)"
    - path: "frontend/src/components/TopFlips.tsx"
      provides: "Flip recommendations panel (FRNT-03)"
    - path: "frontend/src/services/api.ts"
      provides: "axios wrappers for /api/bazaar and /api/flips"
    - path: "frontend/src/types/api.ts"
      provides: "TypeScript interfaces matching DTOs"
  key_links:
    - from: "frontend/src/services/api.ts"
      to: "http://localhost:5000/api/*"
      via: "axios instance"
      pattern: "axios.get.*api/bazaar|api/flips"
    - from: "frontend/src/components/ItemsList.tsx"
      to: "frontend/src/services/api.ts"
      via: "import and useFetchBazaar"
      pattern: "useFetchBazaar|api.getBazaar"
    - from: "frontend/src/components/PriceChart.tsx"
      to: "frontend/src/services/api.ts"
      via: "useEffect fetching history"
      pattern: "api.getItemHistory"
    - from: "frontend/src/components/TopFlips.tsx"
      to: "frontend/src/services/api.ts"
      via: "import and useFetchFlips"
      pattern: "api.getTopFlips"
---

<objective>
Create React frontend to display bazaar items, price history charts, and top flip recommendations.

Purpose: Enable users to view bazaar data and identify profitable flips via web UI.

Output: React SPA with dashboard layout, items list, price charts, and flips panel
</objective>

<context>
@.planning/phases/02-api-flip-analysis/02-01-PLAN.md
@.planning/phases/02-api-flip-analysis/02-02-PLAN.md

<!-- Backend API contracts -->
<!-- API runs on http://localhost:5000 -->

<!-- GET /api/bazaar returns: -->
<!-- [ -->
<!--   { -->
<!--     "productId": "ENDER_PEARL", -->
<!--     "name": "Ender Pearl", -->
<!--     "currentBuyPrice": 10.50, -->
<!--     "currentSellPrice": 12.25, -->
<!--     "buyVolume": 150000, -->
<!--     "sellVolume": 145000, -->
<!--     "lastUpdated": "2026-03-28T12:00:00Z" -->
<!--   } -->
<!-- ] -->

<!-- GET /api/bazaar/{productId}/history?start={date}&end={date} returns: -->
<!-- { -->
<!--   "productId": "ENDER_PEARL", -->
<!--   "productName": "Ender Pearl", -->
<!--   "snapshots": [ -->
<!--     { "timestamp": "2026-03-28T12:00:00Z", "buyPrice": 10.50, "sellPrice": 12.25, "buyVolume": 150000, "sellVolume": 145000 } -->
<!--   ] -->
<!-- } -->

<!-- GET /api/flips?count=50&minProfitPercent=1.0 returns: -->
<!-- { -->
<!--   "flips": [ -->
<!--     { -->
<!--       "productId": "ENDER_PEARL", -->
<!--       "productName": "Ender Pearl", -->
<!--       "buyPrice": 10.50, -->
<!--       "sellPrice": 12.25, -->
<!--       "profitMargin": 1.75, -->
<!--       "profitPercentage": 16.67, -->
<!--       "volumeScore": 5.2, -->
<!--       "recommendationScore": 9.1 -->
<!--     } -->
<!--   ], -->
<!--   "generatedAt": "2026-03-28T12:00:00Z", -->
<!--   "totalItemsAnalyzed": 100 -->
<!-- } -->

<!-- DTO TypeScript equivalents (create in types/api.ts):
- BazaarItem: { productId: string; name: string; currentBuyPrice: number; currentSellPrice: number; buyVolume: number; sellVolume: number; lastUpdated: string }
- PriceSnapshot: { timestamp: string; buyPrice: number; sellPrice: number; buyVolume: number; sellVolume: number }
- PriceHistory: { productId: string; productName: string; snapshots: PriceSnapshot[] }
- FlipRecommendation: { productId: string; productName: string; buyPrice: number; sellPrice: number; profitMargin: number; profitPercentage: number; volumeScore: number; recommendationScore: number }
- FlipsResponse: { flips: FlipRecommendation[]; generatedAt: string; totalItemsAnalyzed: number }
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up React project with Vite</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/index.html, frontend/tsconfig.json</files>
  <action>
Create frontend directory with Vite React TypeScript project:

1. **frontend/package.json** with dependencies:
   - react, react-dom
   - axios (for API calls)
   - chart.js, react-chartjs-2 (for price history charts)
   - react-router-dom (for navigation)
   - vite, @vitejs/plugin-react (dev)

2. **frontend/vite.config.ts**:
   - Configure proxy to backend: `/api` → `http://localhost:5000/api`
   - This allows frontend to call `/api/*` without CORS issues

3. **frontend/index.html**:
   - Basic HTML with root div
   - Title: "SkyBazaar - Hypixel SkyBlock Flip Finder"

4. **frontend/tsconfig.json**: Standard React TypeScript config

5. **frontend/src/main.tsx**: React entry point rendering App

6. **frontend/src/App.tsx**: Basic layout with dark theme CSS
</action>
  <verify>
<automated>cd frontend && npm install 2>&1 | tail -10</automated>
  </verify>
  <done>Vite React project created and installs dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create API service and TypeScript types</name>
  <files>frontend/src/types/api.ts, frontend/src/services/api.ts</files>
  <action>
Create TypeScript interfaces matching backend DTOs and axios API client:

**frontend/src/types/api.ts:**
- `BazaarItem` - matches GET /api/bazaar response item
- `PriceSnapshot` - timestamp, buyPrice, sellPrice, buyVolume, sellVolume
- `PriceHistory` - productId, productName, snapshots[]
- `FlipRecommendation` - productId, productName, buyPrice, sellPrice, profitMargin, profitPercentage, volumeScore, recommendationScore
- `FlipsResponse` - flips[], generatedAt, totalItemsAnalyzed

**frontend/src/services/api.ts:**
- Create axios instance with baseURL from environment (default: '')
- `getBazaar()` - GET /api/bazaar
- `getItemHistory(productId, start?, end?)` - GET /api/bazaar/{productId}/history
- `getTopFlips(count?, minProfitPercent?)` - GET /api/flips
- `getFlipForItem(productId)` - GET /api/flips/{productId}
- Export typed response handlers
</action>
  <verify>
<automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>API client ready with TypeScript types matching backend DTOs</done>
</task>

<task type="auto">
  <name>Task 3: Create Sidebar and Items list components</name>
  <files>frontend/src/components/Sidebar.tsx, frontend/src/components/ItemsList.tsx</files>
  <action>
Create sidebar navigation and items list with search:

**frontend/src/components/Sidebar.tsx:**
- Fixed sidebar on left (250px width)
- Navigation items: "Items", "Flips", "History"
- Active state styling (highlighted)
- Dark theme background (#1a1a2e)
- Click handlers to switch views

**frontend/src/components/ItemsList.tsx (FRNT-01, FRNT-04):**
- Search input at top: "Search items by name..."
- Loading state while fetching
- Table/grid displaying all bazaar items:
  - Product name
  - Buy price (formatted as coins)
  - Sell price (formatted as coins)
  - Volume (formatted: 1.5M, 2.3K, etc.)
- Click row to select item and show price chart
- Filter items by name as user types (FRNT-04)
- Empty state: "No items match your search"
</action>
  <verify>
<automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Sidebar navigation and searchable items list implemented</done>
</task>

<task type="auto">
  <name>Task 4: Create PriceChart component</name>
  <files>frontend/src/components/PriceChart.tsx</files>
  <action>
Create price history chart component (FRNT-02):

**frontend/src/components/PriceChart.tsx:**
- Use Chart.js with react-chartjs-2
- Line chart with two datasets:
  - Buy price (color: #ff6b6b - red)
  - Sell price (color: #4ecdc4 - teal)
- X-axis: timestamp
- Y-axis: price (coins)
- Tooltip showing exact values
- Legend: "Buy Price", "Sell Price"
- Title: "{Item Name} - Price History"
- Loading state while fetching
- Empty state: "Select an item to view price history"
- Time range selector: "24h", "7d", "30d", "All" (updates API query params)
</action>
  <verify>
<automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Price history line chart renders with buy/sell lines</done>
</task>

<task type="auto">
  <name>Task 5: Create TopFlips component</name>
  <files>frontend/src/components/TopFlips.tsx</files>
  <action>
Create top flip recommendations panel (FRNT-03):

**frontend/src/components/TopFlips.tsx:**
- Fetch from GET /api/flips on mount
- Display as ranked list (best flip first)
- Each flip card shows:
  - Product name
  - Buy → Sell prices
  - Profit margin (formatted: "+1.75 coins")
  - Profit percentage (formatted: "+16.67%")
  - Recommendation score (color-coded: green > 7, yellow 4-7, red < 4)
- Sort by recommendationScore descending
- Click flip to see its price history chart
- Refresh button to re-fetch latest flips
- Loading state and error handling
</action>
  <verify>
<automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Top flips panel displays ranked profit opportunities</done>
</task>

<task type="auto">
  <name>Task 6: Integrate all components in App</name>
  <files>frontend/src/App.tsx</files>
  <action>
Wire up all components in main App with state management:

**frontend/src/App.tsx:**
- State: currentView ('items' | 'flips'), selectedItem (string | null)
- Layout: Sidebar (left) + Main content (right)
- Main content conditional:
  - If view === 'items': Show ItemsList + PriceChart (selected item)
  - If view === 'flips': Show TopFlips (clicking item shows chart)
  - If view === 'history': Show selected item's chart
- Pass selectedItem state down to show chart when item clicked
- Add global CSS for dark theme:
  - Background: #0f0f1a (dark navy)
  - Cards: #1a1a2e
  - Text: #e0e0e0
  - Accent: #6c5ce7 (purple)
  - Positive: #00b894 (green)
  - Negative: #ff7675 (red)

Final integration test:
- Verify all components render
- Verify API calls work via proxy
- Verify navigation between views
</action>
  <verify>
<automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Complete frontend with working navigation and data display</done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds in frontend/
- [ ] Items list shows bazaar items from API
- [ ] Search filters items by name
- [ ] Price chart renders for selected item
- [ ] Top flips shows ranked recommendations
- [ ] Dark theme applied consistently
- [ ] Navigation between Items/Flips works
</verification>

<success_criteria>
Frontend displays bazaar items, price history charts, and top flips. All 4 requirements (FRNT-01, FRNT-02, FRNT-03, FRNT-04) satisfied. User can search items, view charts, and see profit opportunities.
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend/03-01-SUMMARY.md`
</output>
